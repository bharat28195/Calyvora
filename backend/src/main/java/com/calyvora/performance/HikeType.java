package com.calyvora.performance;

/** How a manager expressed the raise: a percentage of current pay, an absolute new salary, or none. */
public enum HikeType {
    PERCENT,
    NEW_SALARY,
    NONE
}
